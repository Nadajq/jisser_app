import 'package:flutter/material.dart';

class SpecialistInfoPage extends StatelessWidget {
  const SpecialistInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Specialist Info Page")),
    );
  }
}
