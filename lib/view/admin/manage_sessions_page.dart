import 'package:flutter/material.dart';

class ManageSessionsPage extends StatelessWidget {
  const ManageSessionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Manage Sessions Page')),
    );
  }
}
