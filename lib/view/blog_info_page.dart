import 'package:flutter/material.dart';

class BlogInfoPage extends StatelessWidget {
  const BlogInfoPage(String s, MaterialColor blue, String , {super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("blog info page")),
    );
  }
}
