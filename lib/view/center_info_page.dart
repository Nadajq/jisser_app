import 'package:flutter/material.dart';

class CenterInfoPage extends StatelessWidget {
  const CenterInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
    body: Center(child: Text("center info page")),

    );
  }
}
