import 'package:flutter/material.dart';

class SpecialistChatPage extends StatelessWidget {
  const SpecialistChatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Specialist Chat Page")),
    );
  }
}
